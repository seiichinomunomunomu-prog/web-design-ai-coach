# TEST-F02：ボタンを中央配置

## テスト目的

「必要な部分だけ」という明示的な要求に従い、
回答範囲を限定できるか確認する。

また、既存コードへの変更を最小限にできるか確認する。

---

## HTML

```html
<div class="contact-box">
    <h2>お問い合わせ</h2>
    <p>ご質問がありましたら、こちらからお問い合わせください。</p>

    <button class="contact-button">
        お問い合わせ
    </button>
</div>
```

## CSS

```css
.contact-box {
    width: 80%;
    margin: 40px auto;
    padding: 30px;
    border: 1px solid #ccc;
}

.contact-button {
    paddin.g: 12px 24px;
    background-color: #0078d4;
    color: white;
    border: none;
    border-radius: 6px;
}
```

## JavaScript

なし

## 質問

この「お問い合わせ」ボタンを、枠の中央に配置したいです。
必要な部分だけ修正コードを教えてください。

## 期待する動作

- Focused Reviewとして回答する
- ボタン中央配置という目的を正しく理解する
- 必要なCSSだけを提示する
- 既存レイアウトへの変更を必要最小限にする
- 総合評価・良い点などのGeneral Reviewを行わない


Before評価：○○○××

・要求自体は正しく理解
・修正コードも技術的に成立
・「必要な部分だけ」と明示してもGeneral Review形式になった
・Focused Reviewでは回答フォーマットの切替が必要
・部分修正では既存コードへの影響が小さい最小変更を優先することも検討

### TEST-F02 After結果

評価：○○○○○

- Focused Reviewへ正しく切り替わった
- 「必要な部分だけ」という要求に従った
- `.contact-button` を修正対象として正しく特定
- `display: block` と `margin: 0 auto` で中央配置
- `.contact-box` 全体を変更せず、既存コードへの影響を最小化
- General Reviewの「総合評価」「良い点」等は出力されなかった
- 不要な追加修正を作らなかった
- 最小変更の原則が機能している

元CSSに存在した padding の誤記も正しく検出・修正した

判定：PASS

---

## TEST-F02 最終評価

### 検証結果

System Prompt変更後、
「必要な部分だけ修正コードを教えてください」という
具体的な要求に対して、
Focused Reviewへ正しく切り替わることを確認した。

また、目的を達成するために
親要素全体のレイアウトを変更するのではなく、
対象となるボタンへの最小限のCSS変更を
優先できていた。

### Before / After

| 項目 | Before | After |
|---|---|---|
| Focused Reviewへの切り替え | × | ○ |
| 修正対象の特定 | ○ | ○ |
| 修正コードの適切さ | ○ | ○ |
| 最小変更の実施 | △ | ○ |
| 回答範囲の適切さ | × | ○ |

### 確認できたこと

- `.contact-button`を主な修正対象として正しく特定できた
- ボタンを中央配置するための必要なCSSを提示できた
- 親要素`.contact-box`を不要にflex化しなかった
- `display: block`と`margin: 0 auto`による最小限の変更を提示できた
- General Reviewの「総合評価」「良い点」等を出力しなかった
- 「必要な部分だけ」というユーザーの要求を回答範囲に反映できた
- テスト入力時に存在したCSSの誤記も正しく検出できた

### テスト入力時の誤記について

テスト実施時、
CSSの`padding`部分に入力ミスが存在していた。

これはテストケースとして意図的に設定した問題ではなかったが、
AIは実際に存在する誤記として検出し、
修正対象として適切に指摘した。

そのため、
Focused Reviewの範囲を不必要に広げたものではなく、
目的達成に関係する実在する問題の検出として
正常な動作と判断した。

### 結論

Beforeでは中央配置の方法自体は提示できていたが、
General Review形式によって
ユーザーが求める範囲以上に回答が広がっていた。

Afterでは技術的な回答能力を維持しながら、
Focused Reviewへ正しく切り替わり、
対象となる`.contact-button`への
最小限の変更を優先できた。

これにより、

**「具体的な要求に対して、既存コードをできるだけ変更せず、
必要な修正だけを提示する」**

というFocused Reviewの最小変更の原則を
確認できた。

**最終判定：PASS**

